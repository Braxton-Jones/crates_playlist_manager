export default function LandingPage() {
  {
    /* 
    Save Point:
    - We need a way to get the access token, refresh token, and expires in from the url
    - We need to pass the access token, refresh token, and expires in to the dashboard page
    - We need to make it so the token is refreshed when it expires
    - We need to make it so the user can't access the dashboard page without the access token, refresh token, and expires in
    - We need to make it so the user can't access the login page if they already have the access token, refresh token, and expires in  
    */
  }
  return (
    <main>
      <h2>Welcome to Crates pt2</h2>
    </main>
  );
}
